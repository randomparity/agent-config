repos:
  - repo: local
    hooks:
      - id: commit-check
        name: just lint format-check public-safety
        entry: just lint format-check public-safety
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit]
