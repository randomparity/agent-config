repos:
  - repo: local
    hooks:
      - id: commit-check
        name: just commit-check
        entry: just commit-check
        language: system
        always_run: true
        pass_filenames: false
        stages: [pre-commit]
