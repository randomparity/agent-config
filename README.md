# agent-config (archived)

Superseded 2026-08-11 by:

- **[randomparity/adept](https://github.com/randomparity/adept)** — the skills,
  as a Claude Code plugin marketplace and Codex plugin. See its ADR 0001 for
  why the installer was retired.
- **randomparity/dotfiles** (private) — agent configuration, chezmoi-owned
  whole files. No overlay merge exists anymore.

History, ADRs 0001–0057, and debt records remain browsable here.

The 37 issues open at archival were dispositioned rather than abandoned. Those
whose subject was the installer, the overlay protocol, or a retired deployment
gate are closed as obsolete, with the reason on each; the rest moved to
whichever successor now owns their surface. Pull request #175 is closed
unmerged, and its diff is still the right starting point for the bug it fixes —
the issue that carries that bug says so.
